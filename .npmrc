# Flatten node_modules for Expo's Metro bundler — Metro doesn't follow
# pnpm's nested .pnpm/ structure cleanly.
node-linker=hoisted
public-hoist-pattern[]=*
shamefully-hoist=true
strict-peer-dependencies=false
